name: Release Executables

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  create_release:
    name: Create Release
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Verify package version
        shell: bash
        run: python -c "import os, tomllib; version = tomllib.load(open('pyproject.toml', 'rb'))['project']['version']; assert version == os.environ['GITHUB_REF_NAME'][1:]"

      - name: Build Python package
        shell: bash
        run: |
          python -m pip install --upgrade pip build
          python -m unittest discover -v
          python -m build

      - name: Create GitHub release notes
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: dist/*

  build:
    name: Build ${{ matrix.platform_name }}
    needs: create_release
    if: always() && (needs.create_release.result == 'success' || needs.create_release.result == 'skipped')
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            platform_name: Linux
          - os: windows-latest
            platform_name: Windows
          - os: macos-latest
            platform_name: macOS

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        shell: bash
        run: |
          python -m pip install --upgrade pip
          python -m pip install . "pyinstaller>=6.0,<7"

      - name: Verify tkinter for GUI build
        shell: bash
        run: python -c "import tkinter"

      - name: Resolve artifact metadata
        id: meta
        shell: bash
        run: |
          arch="$RUNNER_ARCH"
          case "$arch" in
            X64) arch="x86_64" ;;
            ARM64) arch="aarch64" ;;
            *) arch="$(echo "$arch" | tr '[:upper:]' '[:lower:]')" ;;
          esac
          echo "arch=$arch" >> "$GITHUB_OUTPUT"

      - name: Build command-line executable
        shell: bash
        run: |
          python -m PyInstaller \
            --noconfirm \
            --clean \
            --onefile \
            --paths . \
            --distpath dist \
            --workpath build/command-line \
            --specpath build/spec \
            --name "Chomikuj-Magic-Command-Line-${{ matrix.platform_name }}-${{ steps.meta.outputs.arch }}" \
            chomikuj/cli.py

      - name: Build graphical executable
        shell: bash
        run: |
          python -m PyInstaller \
            --noconfirm \
            --clean \
            --onefile \
            --windowed \
            --paths . \
            --distpath dist \
            --workpath build/graphical \
            --specpath build/spec \
            --name "Chomikuj-Magic-GUI-${{ matrix.platform_name }}-${{ steps.meta.outputs.arch }}" \
            chomikuj/gui.py

      - name: Upload files to GitHub release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
